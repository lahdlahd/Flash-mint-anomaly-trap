# Flash-mint-anomaly-trap
 built a flash mint spike monitor - what it does this trap is designed for test tokens, especially those that support flash minting, a feature that temporarily increases a token’s total supply within a single transaction or block.
