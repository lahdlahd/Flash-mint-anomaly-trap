ethereum_rpc = ""
drosera_rpc = ""
drosera_address = ""

[traps]

[traps.flash_mint_spike_monitor]
path = "out/FlashMintSpikeTrap.sol/FlashMintSpikeTrap.json"

# Response contract (put your deployed responder address here)
response_contract = "0x0000000000000000000000000000000000000000"

# Function your responder will call when a spike is detected
response_function = "handleFlashMintSpike(uint256,uint256)" 
# Arguments: (currentSupply, spikePercent)

# How many blocks to observe at once
block_sample_size = 3

# Cooldown period before responding again
cooldown_period_blocks = 10

# Operator requirements
min_number_of_operators = 1
max_number_of_operators = 3

# Trap visibility / access
private_trap = false
whitelist = []

# Deployed trap address (update after deploying)
address = "0x0000000000000000000000000000000000000000"
